<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/profile.css">
    <link rel="stylesheet" href="/styles/index.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Profile</title>
</head>
<body>
    

    <div class="container-fluid">
        <nav class="navbar navbar-expand-sm bg-light navbar-light ">
            <div class="container-fluid">
                <a class="navbar-brand" href="/"><img src="../public/assets/logo.png" class="rounded logo"></a>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/search">Find</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/profile">Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                </ul>
            </div>
        </nav>

    
      <br/>
    
    <div class="container-fluid">
        <div class="row">
            <div class="col"><h1><%= user.username %></h1></div>
        </div>
        <br>
        <div class="row">
            <div class="col"> </div>
            <div class="col"><h4>Biography</h4></div>
            <p><strong>Github Bio:</strong> <%= user.bio %></p>
            <p><strong>Age:</strong> <%= personalInfo.age || 'N/A' %></p>
        </div>
        
        <div class="row">
            <div class="col"><img src="<%= user.avatar_url %>" class="rounded-circle pfp"></div>
            <div class="col"><%= personalInfo.age || 'N/A' %>
            </div>
        </div>
    </div> 
    <br>
    <br>
    <div class="container-fluid">
        <div class="row">
            <div class="col">
                
                <p><strong>Username:</strong> <%= user.username %></p>
                <p><strong>Age:</strong> <%= personalInfo.age || 'N/A' %></p>
                <p><strong>Gender:</strong> <%= personalInfo.gender || 'N/A' %></p>
                <p><strong>Likes:</strong> <%= personalInfo.likes || 'N/A' %></p>
                <p><strong>Skills:</strong> <%= personalInfo.skills || 'N/A' %></p>
                <p><strong>Location:</strong> <%= personalInfo.location || 'N/A' %></p>
                <p><strong>Looking for:</strong> <%= personalInfo.looking_for || 'N/A' %></p>
            </div>
        </div>
    </div>  
</body>
</html>
<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Your Profile</title>
</head>
<body>

    <div class="container-fluid">
        <nav class="navbar navbar-expand-sm bg-light navbar-light ">
            <div class="container-fluid">
                <a class="navbar-brand" href="/"><img src="../public/assets/logo.png" class="rounded logo"></a>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/search">Find</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/profile">Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Dashboard</a>
                    </li>
                </ul>
            </div>
        </nav>

    <header>
        <h1>Your Profile</h1>
    </header>
    <main>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Age:</strong> <%= personalInfo.age || 'N/A' %></p>
        <p><strong>Gender:</strong> <%= personalInfo.gender || 'N/A' %></p>
        <p><strong>Likes:</strong> <%= personalInfo.likes || 'N/A' %></p>
        <p><strong>Skills:</strong> <%= personalInfo.skills || 'N/A' %></p>
        <p><strong>Location:</strong> <%= personalInfo.location || 'N/A' %></p>
        <p><strong>Looking for:</strong> <%= personalInfo.looking_for || 'N/A' %></p>
    </main>
</body>
</html> -->